name: Build & Deploy on Push

on:
  push:
    branches: [ main, master ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  call-build-deploy:
    uses: wjlucc/linespark/.github/workflows/build_deploy.yml@main